import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFF1A202C); // Main background
  static const Color backgroundDark = Color(0xFF0B0E11); // Darker background (e.g. scanner screen)
  static const Color surface = Color(0xFF111827); // AppBar, Cards
  static const Color surfaceLight = Color(0xFF1F2937); // Borders, slight highlights
  
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.white70;
  static const Color textMuted = Colors.grey;

  static const Color primary = Colors.blue;
  static const Color success = Colors.green;
  static const Color error = Colors.redAccent;
  static const Color warning = Colors.orangeAccent;
}
